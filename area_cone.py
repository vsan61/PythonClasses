# write program to find area of the cone with radius 250
pi = 3.14

radius = int(input("Enter the radius of the cone"))

height = float(input("Enter the height of the cone"))

Area = 0.33*pi*radius*radius*height

print(f"Area of the cone with {radius} and {height} is {Area}")

